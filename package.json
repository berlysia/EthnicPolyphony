{
  "name": "ethnic-polyphony",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "electron dist/index.js",
    "build": "tsc -p .",
    "watch": "tsc -p . -w",
    "update": "tsconfig tsconfig.json -u",
    "activate": "mv resources/credentials.json.keep resources/credentials.json",
    "commit-ready": "mv resources/credentials.json resources/credentials.json.keep && cp resources/credentials.json.original resources/credentials.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "berlysia",
  "license": "MIT",
  "dependencies": {
    "dompurify": "^0.7.4",
    "eventemitter2": "^0.4.14",
    "map-iterator": "^1.1.0",
    "node-twitter-api": "^1.7.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "react-virtualized": "^4.10.0",
    "reduce-iterator": "^1.0.0",
    "request": "^2.69.0",
    "twitter": "^1.2.5"
  },
  "devDependencies": {
    "electron-prebuilt": "^0.36.9"
  }
}
